你现在是一个经验丰富的 Solana 全栈开发者，同时精通 Anchor、Rust、TypeScript、Electron 和 Metaplex。

项目背景：

- 这是一个桌面应用（Electron），目标是记录用户的电脑使用情况（CPU/内存/进程等），只把哈希上链保护隐私。
- 链上使用 Solana + Anchor 程序
- 隐私策略：中等隐私，只上链哈希 + 少量明文汇总统计（日平均 CPU/内存、Top5 进程名）
- 设备唯一性：每台电脑生成一个 deviceHash（用 systeminformation 库采集硬件信息哈希）
- 同一钱包允许绑定多台电脑，每个设备独立注册并 mint 一个灵魂绑定 NFT
- 数据采集：每小时采集一次快照，每天凌晨批量上链一次（24 小时数据）

当前技术栈已确定：

- 后端：Anchor 0.30+ (Rust) + PDA
- 前端/桌面：Electron + TypeScript + systeminformation
- Solana 交互：@solana/web3.js + @coral-xyz/anchor + @metaplex-foundation/js
- 哈希：SHA-256（可加钱包地址作为 salt）

请你现在帮我完成以下具体任务：
[在这里填入你当前最想让它帮你做的具体事项，例如：]

1. 帮我写完整的 Anchor 程序，包括：

   - register_device 指令（用 device_hash 做 PDA seeds，确保同一设备只能注册一次）
   - upload_daily_usage 指令（存当天汇总统计 + 整体 payload 的 SHA256 哈希）
   - 合适的 account 结构和错误码

2. 或者：帮我写 Electron 主进程中定时采集使用数据的代码（每小时一次），并在每天凌晨把 24 小时数据打包计算哈希准备上链

3. 或者：设计 NFT mint 的完整流程（包括 metadata 建议），在设备首次注册时自动 mint 灵魂绑定 NFT

请按照以下结构输出你的回答：

1. 先给出整体思路和关键设计决策（3-8 句话）
2. 再给出完整、可直接使用的代码（Rust / TypeScript）
3. 最后说明可能遇到的坑 + 如何测试/调试
4. 如果需要我提供更多上下文或已有代码，请告诉我哪些部分

保持代码风格干净、注释清晰，优先考虑 gas 优化、安全性（尤其是 PDA 种子设计和签名验证）。
开始吧！
